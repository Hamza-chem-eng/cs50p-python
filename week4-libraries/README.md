# Week 4: Libraries

- safetyAdvisor.py — Chemical safety advisor using cowsay, random, and sys.argv
- tempSensorSim.py — Temperature sensor simulator with random and input validation
- tempStatsAnalyzer.py — Temperature statistics analyzer using statistics (mean, mode) and sys.argv

## What I learned

- Working with external libraries (cowsay)
- sys.argv for command-line arguments
- random module for simulations
- statistics module — mean() and mode() for data analysis